import { FromToDirective } from './from-to.directive';

describe('FromToDirective', () => {
  it('should create an instance', () => {
    const directive = new FromToDirective();
    expect(directive).toBeTruthy();
  });
});
